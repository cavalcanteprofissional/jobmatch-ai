# URL da API JobMatch (Render ou local)
# Se definido, o frontend tenta esta URL primeiro e fallback para localhost:8000
# Se vazio, usa /api (proxy Vite → localhost:8000)
VITE_API_URL=https://your-api-url.onrender.com
